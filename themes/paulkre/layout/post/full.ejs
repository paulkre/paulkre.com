<div class="post-full">
	<h1 class="title"><%= page.title %></h1>
	
	<% if (page.layout === "post") { %>
		<%- partial("time", {post: page}) %>
	<% } %>
	
	<div class="post-content<%= page.centered ? " centered" : "" %>">
		<%- page.content %>
	</div>
</div>